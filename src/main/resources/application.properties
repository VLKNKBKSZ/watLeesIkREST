spring.datasource.url = jdbc:mysql://localhost:3306/watleesik?useSSL=false
spring.datasource.username=root
spring.datasource.password=admin
spring.jpa.show-sql=true
spring.jpa.hibernate.ddl-auto=none
spring.jpa.hibernate.naming-strategy = org.hibernate.cfg.ImprovedNamingStrategy
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQL8Dialect

#logging
logging.level.nl.watleesik=DEBUG

#Security
security.secret = Tg%43HjD^hgGsyrw(j
security.expirationTime = 864000000
security.tokenPrefix = Bearer 
security.headerString = Authorization
security.signUpUrl = /login
security.registerUrl = /account/register

# Needs check if this is the right approach:
# This seems to prevent the com.fasterxml.jackson.databind.exc.InvalidDefinitionException
#spring.jackson.serialization.fail-on-empty-beans=false
